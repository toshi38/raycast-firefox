---
phase: 13-raycast-store-submission
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - raycast-extension/src/lib/constants.ts
  - raycast-extension/src/search-tabs.tsx
  - raycast-extension/src/setup-bridge.tsx
  - raycast-extension/package.json
  - raycast-extension/assets/icon.png
  - raycast-extension/README.md
  - raycast-extension/LICENSE
  - raycast-extension/CHANGELOG.md
autonomous: false
requirements:
  - STORE-02
  - STORE-03
  - STORE-05
  - STORE-06
  - STORE-08
  - LINK-02
  - LINK-03

must_haves:
  truths:
    - "AMO URL is a single constant imported by all consumers"
    - "Error state for ExtensionNotInstalled shows correct AMO link"
    - "Extension icon is a 512x512 PNG of the fox design, not the orange placeholder"
    - "Store README has setup instructions linking to AMO"
    - "MIT LICENSE exists in the extension directory"
    - "CHANGELOG follows Raycast Store format"
    - "package.json includes platforms macOS"
  artifacts:
    - path: "raycast-extension/src/lib/constants.ts"
      provides: "Shared AMO_URL constant"
      exports: ["AMO_URL"]
    - path: "raycast-extension/README.md"
      provides: "Store-focused setup guide"
      contains: "Setup"
    - path: "raycast-extension/LICENSE"
      provides: "MIT license"
      contains: "MIT License"
    - path: "raycast-extension/assets/icon.png"
      provides: "512x512 custom extension icon"
    - path: "raycast-extension/CHANGELOG.md"
      provides: "Raycast Store format changelog"
      contains: "Initial Release"
  key_links:
    - from: "raycast-extension/src/search-tabs.tsx"
      to: "raycast-extension/src/lib/constants.ts"
      via: "import { AMO_URL }"
      pattern: "import.*AMO_URL.*from.*lib/constants"
    - from: "raycast-extension/src/setup-bridge.tsx"
      to: "raycast-extension/src/lib/constants.ts"
      via: "import { AMO_URL }"
      pattern: "import.*AMO_URL.*from.*lib/constants"
---

<objective>
Prepare Raycast extension code and metadata files for Store submission.

Purpose: Get all code changes, assets, and metadata files in place so the extension passes `ray lint` and `ray build` and meets Raycast Store requirements.
Output: Updated source files (AMO URL centralized, icon replaced, README/LICENSE/CHANGELOG created/updated, package.json updated).
</objective>

<execution_context>
@/Users/stephen/.claude/get-shit-done/workflows/execute-plan.md
@/Users/stephen/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-raycast-store-submission/13-CONTEXT.md
@.planning/phases/13-raycast-store-submission/13-RESEARCH.md

@raycast-extension/src/search-tabs.tsx
@raycast-extension/src/setup-bridge.tsx
@raycast-extension/package.json
@raycast-extension/CHANGELOG.md

<interfaces>
<!-- Key types and contracts the executor needs. -->

From raycast-extension/src/search-tabs.tsx (line ~521-526):
```typescript
// Current: hardcoded wrong AMO URL
{classifiedError.mode === FailureMode.ExtensionNotInstalled && (
  <Action.OpenInBrowser
    title="Install Webextension"
    url="https://addons.mozilla.org/en-US/firefox/addon/raycast-firefox/"
  />
)}
```

From raycast-extension/src/setup-bridge.tsx (line ~21-22):
```typescript
// Current: local constant, correct URL
const AMO_URL =
  "https://addons.mozilla.org/en-US/firefox/addon/raycast-tab-manager-for-firefox/";
```

From raycast-extension/src/lib/ (existing modules):
- errors.ts — error classification
- setup.ts — setup utilities
- installer.ts — native host installer
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Code changes and metadata files</name>
  <files>
    raycast-extension/src/lib/constants.ts,
    raycast-extension/src/search-tabs.tsx,
    raycast-extension/src/setup-bridge.tsx,
    raycast-extension/package.json,
    raycast-extension/assets/icon.png,
    raycast-extension/README.md,
    raycast-extension/LICENSE,
    raycast-extension/CHANGELOG.md
  </files>
  <action>
    Complete all code changes and metadata file creation in a single pass:

    **1. Create `src/lib/constants.ts`:**
    ```typescript
    export const AMO_URL =
      "https://addons.mozilla.org/en-US/firefox/addon/raycast-tab-manager-for-firefox/";
    ```

    **2. Update `src/search-tabs.tsx`:**
    - Add import: `import { AMO_URL } from "./lib/constants";`
    - Replace hardcoded URL at line ~524 with `{AMO_URL}`
    - Fix title from `"Install Webextension"` to `"Install Firefox Extension"` (title case per @raycast/prefer-title-case)

    **3. Update `src/setup-bridge.tsx`:**
    - Add import: `import { AMO_URL } from "./lib/constants";`
    - Remove local `const AMO_URL = ...` declaration (lines ~21-22)

    **4. Update `package.json`:**
    - Add `"platforms": ["macOS"]` field
    - Keep `author` as `"stelau"` for now (will be fixed in checkpoint task)
    - Remove `prebuild` and `predev` scripts (they write `project-root.txt` which breaks in raycast/extensions monorepo context; production path resolution from Phase 9+12 does not need this file). Keep `dev`, `build`, `lint` scripts.

    **5. Replace icon:**
    - Source: `~/Downloads/Gemini_Generated_Image_w1tmzww1tmzww1tm.png` (2816x1536)
    - Use `sips` to crop the icon region from the center of the image. The icon with its rounded-rect border is approximately 1100px tall, centered in the image. Estimate crop area, then resize to 512x512.
    - Commands:
      ```bash
      sips --cropToHeightWidth 1100 1100 --cropOffset 218 858 \
        ~/Downloads/Gemini_Generated_Image_w1tmzww1tmzww1tm.png \
        --out /tmp/icon-cropped.png
      sips --resampleHeightWidth 512 512 /tmp/icon-cropped.png \
        --out raycast-extension/assets/icon.png
      ```
    - Verify dimensions are exactly 512x512 with `sips -g pixelHeight -g pixelWidth raycast-extension/assets/icon.png`
    - If crop looks off (fox not centered), adjust cropOffset values and retry. The goal is to capture the rounded-rect icon shape with the fox + tab design centered.

    **6. Create `README.md`:**
    Write a short, store-focused README per user decision. Include:
    - 1-line description
    - Setup steps: (1) install Firefox extension from AMO, (2) run Setup Firefox Bridge command
    - Privacy note about localhost-only communication
    - Use the actual AMO URL (https://addons.mozilla.org/en-US/firefox/addon/raycast-tab-manager-for-firefox/)

    **7. Copy `LICENSE`:**
    Copy the root `LICENSE` file to `raycast-extension/LICENSE` (copy, not symlink -- symlinks break in monorepo context per research).

    **8. Reformat `CHANGELOG.md`:**
    Rewrite to Raycast Store format:
    ```markdown
    # firefox-tabs Changelog

    ## [Added Setup Command and Native Host Bundling] - 2026-03-14

    - One-command setup: run "Setup Firefox Bridge" to install everything automatically
    - Native host bundled as single file with automatic download from GitHub Releases
    - SHA256 verification of downloaded bundles
    - 5-level Node.js discovery chain for reliable script execution

    ## [Initial Release] - {PR_MERGE_DATE}

    - Search and switch open Firefox tabs by title or URL
    - Close tabs from the action panel
    - Container tab indicators with color coding
    - Multi-window support with window number badges
    - Pinned and active tab indicators
    - Favicon display with letter-avatar fallbacks
    ```
  </action>
  <verify>
    <automated>cd raycast-extension && test -f src/lib/constants.ts && grep -q "AMO_URL" src/lib/constants.ts && grep -q "import.*AMO_URL.*from.*./lib/constants" src/search-tabs.tsx && grep -q "import.*AMO_URL.*from.*./lib/constants" src/setup-bridge.tsx && grep -q '"platforms"' package.json && test -f README.md && test -f LICENSE && grep -q "Initial Release" CHANGELOG.md && sips -g pixelHeight -g pixelWidth assets/icon.png 2>&1 | grep -q "512"</automated>
  </verify>
  <done>
    - constants.ts exports AMO_URL, both consumers import from it
    - search-tabs.tsx uses correct AMO URL and title-cased action title
    - setup-bridge.tsx has no local AMO_URL declaration
    - package.json has platforms field and no prebuild/predev scripts
    - icon.png is 512x512 fox design
    - README.md has setup instructions with AMO link
    - LICENSE is MIT
    - CHANGELOG.md follows Raycast Store format
  </done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 2: Confirm Raycast username and icon rendering</name>
  <decision>Two things need user verification before final lint/build can pass</decision>
  <context>
    1. **Raycast username**: The current `author: "stelau"` in package.json fails `ray lint` validation (404 from Raycast API). The author must match the exact Raycast account username. This blocks ALL lint fixes (author validation runs first).
    2. **Icon on light theme**: The fox icon was cropped from a dark-background source image. Need to verify it renders acceptably on Raycast's light theme. If it looks like a dark blob, options are: (a) provide `icon@dark.png` variant, (b) use icon.ray.so to generate a theme-safe icon.
  </context>
  <options>
    <option id="provide-username">
      <name>Provide correct Raycast username</name>
      <pros>Unblocks ray lint</pros>
      <cons>None</cons>
    </option>
  </options>
  <resume-signal>Provide your Raycast username (check Raycast > Settings > Account) and confirm if the icon looks acceptable on both light and dark themes</resume-signal>
</task>

</tasks>

<verification>
- `grep -r "addons.mozilla.org/en-US/firefox/addon/raycast-firefox/" raycast-extension/src/` returns NO results (old URL gone)
- `grep -r "AMO_URL" raycast-extension/src/` shows only imports and the constants.ts definition
- `sips -g pixelHeight -g pixelWidth raycast-extension/assets/icon.png` shows 512x512
- `test -f raycast-extension/README.md && test -f raycast-extension/LICENSE`
</verification>

<success_criteria>
All code changes complete, all metadata files in place, ready for lint/build validation once Raycast username is confirmed.
</success_criteria>

<output>
After completion, create `.planning/phases/13-raycast-store-submission/13-01-SUMMARY.md`
</output>
